# Microservice: data-connector
# ConHub Data Ingestion Service

# =============================================================================
# Core Service Configuration
# =============================================================================
PORT=3013
RUST_LOG=data_service=info,actix_web=info

# =============================================================================
# Auth0 JWT Validation (shared auth middleware)
# =============================================================================
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_ISSUER=https://your-tenant.auth0.com/
AUTH0_AUDIENCE=https://api.your-domain.com
AUTH0_JWKS_URI=https://your-tenant.auth0.com/.well-known/jwks.json

# =============================================================================
# Database (Neon PostgreSQL)
# =============================================================================
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# =============================================================================
# Redis (for caching and job queues)
# =============================================================================
# REDIS_URL=rediss://user:password@host:port

# =============================================================================
# Service Integration URLs (configurable for cloud deployment)
# =============================================================================
# Auth service for internal token resolution
AUTH_SERVICE_URL=http://localhost:3010

# Chunker service for document processing pipeline
CHUNKER_SERVICE_URL=http://localhost:3017

# Embedding service integration
EMBEDDING_SERVICE_URL=http://localhost:8082
EMBEDDING_ENABLED=true

# Relation Graph service for knowledge graph construction
RELATION_GRAPH_SERVICE_URL=http://localhost:3018
GRAPH_RAG_ENABLED=true

# =============================================================================
# Zilliz Cloud Vector Database
# =============================================================================
ZILLIZ_PUBLIC_ENDPOINT=https://your-instance.cloud.zilliz.com
ZILLIZ_API_KEY=your_api_key_here
ZILLIZ_COLLECTION=conhub_embeddings
EMBEDDING_DIMENSION=1024

# =============================================================================
# External Search APIs (Jina AI)
# =============================================================================
EMBEDDINGS_API_URL=https://api.jina.ai/v1/embeddings
EMBEDDINGS_MODEL=jina-embeddings-v3
RERANK_API_URL=https://api.jina.ai/v1/rerank
RERANK_MODEL=jina-reranker-v2-base-multilingual
READER_BASE_URL=https://r.jina.ai
EXTERNAL_SEARCH_API_KEY=your_jina_api_key

# =============================================================================
# GitHub App Configuration (optional)
# =============================================================================
GITHUB_APP_NAME=your-github-app
# GITHUB_APP_ID=
# GITHUB_APP_PRIVATE_KEY_PATH=

# =============================================================================
# Local File Sync (development)
# =============================================================================
# LOCAL_SYNC_PATH_DEFAULT=C:/path/to/your/projects
